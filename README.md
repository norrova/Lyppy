# Lyppy

# Commands
* `lyppy`
    * `-h` : help
    * `-v` : lyppy version
    * `-m` : make
        * `-p` : make project
        * `-mo` : make modules
# Example
![example](https://i.ibb.co/qnSqRLd/image.png)